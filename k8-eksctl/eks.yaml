apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: localhelp
  region: us-east-1

managenodeGroups:
  - name: expense
    instanceType: m5.large
    desiredCapacity: 3
    spot: true